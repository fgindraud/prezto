#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Editor
export EDITOR=vim
export SUDO_EDITOR=rvim
export DIFFPROG=meld

# Custom
alias o='kde-open'
alias backlight='xrandr --output LVDS1 --set BACKLIGHT'
#alias vim='vim 2>/dev/null' # temporary fix to the libclang / clang_autocomplete ncurse bug
alias vimo='vim -O'

# Local path
path+=~/.local/bin
path=($^path(N))

# Reboot / Halt
function uhalt {
	poweroff
	exit
}
function ureboot {
	reboot
	exit
}

